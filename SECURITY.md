# 安全说明

此项目目前用于本地演示和授权测试，**不是生产级安全处置系统**。默认无认证的服务只能用于本机；公开网络部署需要独立威胁建模、可靠身份服务、密钥管理、数据治理和部署审查。`python -m app` 的非回环绑定检查不覆盖直接 `uvicorn` 启动。审批示例只有 dry-run，不得据此接入真实封禁。

发现疑似漏洞时，不要发布含利用细节、真实事件、token 或个人信息的公开 issue。请通过项目所有者认可的**私下渠道**提供最小复现、影响范围和版本；项目尚未指定公开安全邮箱或响应 SLA。维护方确认后再讨论披露。不要为了验证漏洞访问未经授权的系统或数据。

本地文件 `.env`、SQLite 数据库、`data/` 和生成报告由使用者自行保管；`.gitignore` 只是防误提交措施，不是加密或访问控制。贡献前检查待提交文件、日志和截图是否含敏感数据。最小权限、租户隔离、审批及证据引用应维持原有边界，不要通过禁用保护来绕过失败。
